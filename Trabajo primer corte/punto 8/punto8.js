

let x= parseInt(prompt('ingrese un  número'));
let y = x%2;
if (y == 0)
{
    document.write("El número "+ x + " es divisible entre 2" );
}
else if (y == 1)
{
        document.write("El número "+ x + " no es divisible en 2" );
}
